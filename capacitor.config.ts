import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.vercel.ibz',
  appName: 'ibz',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
